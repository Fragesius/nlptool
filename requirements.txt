# 中文分词与关键词提取
jieba>=0.42.1

# 英文 NLP（词性、依存句法、NER）。安装后需下载模型：
#   python -m spacy download en_core_web_sm
spacy>=3.5.0
click>=8.0.0  # spaCy 下载命令依赖

# 绘图与词云
matplotlib>=3.5.0
wordcloud>=1.9.0

# 现代化 GUI 框架（基于 tkinter，深浅色主题跟随系统）
customtkinter>=5.2

# 中文情感分析（可选，缺失时自动回退到内置词典）
snownlp>=0.12.3

# 英文情感分析 / 语料（可选，缺失时自动回退到内置词典）
nltk>=3.8.0

# 文件读取（可选，缺失时相关格式不支持）
python-docx>=0.8.11
PyPDF2>=3.0.0

# 可选 API 调用（高级分析）
requests>=2.28.0

# 开发依赖（测试）
pytest>=7.0.0
